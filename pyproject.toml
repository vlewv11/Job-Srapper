[project]
name = "job-scrapper"
version = "0.1.0"
description = "Scrape ML/AI/LLM job postings from Telegram channels into SQLite with a dashboard"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.2",
    "jinja2>=3.1.6",
    "python-dotenv>=1.2.2",
    "telethon>=1.44.0",
    "uvicorn[standard]>=0.51.0",
]
